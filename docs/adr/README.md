# Architecture Decision Records

Create an ADR before changing public API contracts, wire or disk formats, ownership/lifetime rules, thread or shard models, security defaults, dependency boundaries, or release compatibility.

Name records `NNNN-short-title.md`. Accepted records are immutable; superseding decisions create a new ADR and link both directions.
